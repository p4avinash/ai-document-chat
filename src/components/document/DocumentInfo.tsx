import React from "react"

interface IProps {}

const DocumentInfo: React.FC<IProps> = (props) => {
  return <div>DocumentInfo</div>
}

DocumentInfo.displayName = "DocumentInfo"

export default DocumentInfo
